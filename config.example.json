{
  "host": "127.0.0.1",
  "port": 7654,
  "streamAddonUrl": "",
  "_comment_publicBaseUrl": "Set this only if you watch from another device. It must be an address that device can reach, not localhost.",
  "_comment_accessToken": "Required to reach this server from anywhere but the machine it runs on; it becomes the first path segment of the addon URL. Empty means no authentication at all. Generate one with: node -e \"console.log(require('node:crypto').randomBytes(24).toString('base64url'))\"",
  "accessToken": "",
  "_comment_trustedHosts": "Host header values besides loopback allowed to reach the channel editor, such as 192.168.1.58:7654. Only consulted once accessToken is set.",
  "trustedHosts": [],
  "_comment_allowUnauthenticatedNonLoopback": "Fail-closed escape hatch for an intentionally trusted LAN/container boundary. Never use for a public port.",
  "allowUnauthenticatedNonLoopback": false,
  "_comment_guideImports": "Remote guides are HTTPS-only and cannot reach private/local addresses. Set allowPrivateGuideImports only for an intentional LAN guide server. External poster URLs and automatic sources are disabled unless explicitly enabled.",
  "allowPrivateGuideImports": false,
  "allowImportedGuideArtwork": false,
  "allowImportedGuideSources": false,
  "_comment_abuseLimits": "Per-client remote auth/tune throttles and persisted TorBox operation budgets. Increase only after observing normal household usage.",
  "authFailureLimit": 20,
  "authFailureWindowSeconds": 300,
  "authBlockSeconds": 900,
  "tuneRequestLimitPerMinute": 30,
  "debridHourlyOperationLimit": 120,
  "debridDailyOperationLimit": 1000,
  "qualityPreference": [
    "1080p",
    "720p",
    "2160p",
    "480p"
  ],
  "video": {
    "width": 1920,
    "height": 1080,
    "fps": 30,
    "bitrate": "6000k",
    "gopSeconds": 2
  },
  "audio": {
    "bitrate": "128k",
    "sampleRate": 48000,
    "channels": 2
  },
  "hls": {
    "segmentSeconds": 2,
    "listSize": 12
  },
  "idleShutdownSeconds": 120,
  "scheduleHorizonHours": 24,
  "_comment_linkKeeper": "Debrid links expire, so the server tops them up ahead of time. 'watched' only does that for channels playing now or watched within linkKeeperGraceMinutes, which keeps a large lineup from spending your debrid rate limit on channels nobody opens; a cold channel resolves on demand and takes a few seconds to start. Use 'all' to prepare every channel.",
  "linkKeeperScope": "watched",
  "linkKeeperGraceMinutes": 30,
  "_comment_encoder": "auto | nvenc | qsv | vaapi | cpu. Leave on auto unless detection picks wrong.",
  "encoder": "auto",
  "_comment_resolver": "Recommended: run the isolated debrid agent and set debridAgentUrl plus debridAgentToken. Legacy direct TorBox and configured stream addons remain supported.",
  "debridAgentUrl": "",
  "debridAgentToken": "",
  "torboxApiKey": "",
  "indexerUrl": "https://torrentio.strem.fun",
  "maxSizeGb": 25,
  "_comment_smart": "Optional keys for smart channels. Basic Cinemeta rules need none. TMDB accepts an API Read Access Token (preferred) or v3 API Key; imports use their respective credentials.",
  "tmdbReadAccessToken": "",
  "tmdbApiKey": "",
  "mdblistApiKey": "",
  "traktClientId": "",
  "stremioAuthKey": ""
}
